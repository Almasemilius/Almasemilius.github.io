import React from "react";

export default function SubmitButton() {
  return (
    <div>
      <button type="submit" className="border-2 hover:bg-primary text-primary transition-all duration-300 hover:text-quinary shadow-md py-2 px-5 rounded-full"> Send</button>
    </div>
  );
}
